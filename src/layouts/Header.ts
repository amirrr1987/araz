import {createElement} from '@/araz';

const classs = `bg-red aaaaaaaaaaaaaaaaaa`

// import Navbar from '@/components/Navbar'
export default createElement('header', {
    attrs: {
        class: header,
    },
    // children:[Navbar]
})
